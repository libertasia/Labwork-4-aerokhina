import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Labwork-4-aerokhina',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
